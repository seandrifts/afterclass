-- 初始資料
-- 這裡的數字全部只是「初始值」，上線後一律在後台調整，不要回來改這個檔案。
-- 基準：客單 120 元、食材成本率 35%、免單成本率 38%、每日 80 客
--
-- 售價來源為店家菜單（滷蛋 40 / 加麵加飯 10 / 飲料 50 / 燙青菜 40）。
-- 若這些數字其實是「成本」而非「售價」，見 docs/PLAN.md §3.6 的切換說明。

-- ---------------------------------------------------------------
-- 全域設定
-- ---------------------------------------------------------------
update settings set
  shop_name              = '（待填店名）',
  primary_color          = '#E4572E',

  -- 開發完成、店員訓練完、獎品備齊後才手動開啟
  campaign_active        = false,

  default_valid_days     = 21,
  max_coupons_per_visit  = 1,
  card_token_valid_days  = 90,
  dynamic_token_ttl_sec  = 60,
  claim_window_minutes   = 30,
  allow_stack_promo      = false,

  avg_ticket             = 120,
  gross_margin_pct       = 55,
  daily_customers        = 80,

  -- 預估月成本 9,400，設 12,000 留緩衝
  monthly_cost_cap       = 12000,
  cost_cap_action        = 'notify',

  pity_enabled           = true,
  pity_threshold         = 20
where id = 1;


-- ---------------------------------------------------------------
-- 獎項
--
-- weight 總和刻意湊成 1000，機率 = weight / 10 %，換算心算方便。
-- cost 為實際成本（只有老闆看得到），face_value 為感知價值（顯示給客人）。
-- ---------------------------------------------------------------
insert into prizes
  (name, type, face_value, cost, discount_amt, min_spend, max_discount,
   weight, stock, valid_days, terms, color, sort_order, is_active)
values
  -- 大獎 0.30%：做成「下次消費折抵」而非當場免單，強制產生第二次來店
  ('免單（下次消費全免）', 'free_meal', 150, 57, null, 0, 150,
   3, null, 21, '折抵上限 150 元，超過部分需自付。限內用。', '#D62828', 1, true),

  -- 4.00%
  ('飲料一杯', 'item', 50, 18, null, 0, null,
   40, null, 21, '限店內指定飲品，恕不折現。', '#F77F00', 2, true),

  -- 6.00%
  ('燙青菜一份', 'item', 40, 14, null, 0, null,
   60, null, 21, '青菜種類依當日供應為準。', '#4CAF50', 3, true),

  -- 20.00% 主力獎，機率最高的「有感」獎項
  ('滷蛋一顆', 'item', 40, 14, null, 0, null,
   200, null, 21, null, '#E9C46A', 4, true),

  -- 45.00% 墊底實物獎，成本最低
  ('加麵 / 加飯', 'item', 10, 4, null, 0, null,
   450, null, 21, '限搭配主餐使用。', '#A8DADC', 5, true),

  -- 24.70% 唯一的現金券。有門檻，用來推高客單
  ('折 10 元', 'cash', 10, 10, 10, 100, null,
   247, null, 21, '單筆消費滿 100 元可用，不可與其他優惠併用，不找零。', '#457B9D', 6, true);


-- ---------------------------------------------------------------
-- 驗算
--   權重合計       1000
--   名目期望成本   8.80 元 / 客
--   核銷率修正後   3.92 元 / 客（佔客單 3.3%）
--   預估月成本     9,400 元（每日 80 客）
--
-- 核銷率為預估值，上線兩週後改用後台報表算出的真實值。
-- ---------------------------------------------------------------


-- ---------------------------------------------------------------
-- 店員（PIN 請在後台重設，不要沿用這裡的預設值）
-- ---------------------------------------------------------------
insert into staff (name, pin_hash, role, is_active)
values ('老闆', '$2b$10$REPLACE_ME_ON_FIRST_LOGIN', 'owner', true);
